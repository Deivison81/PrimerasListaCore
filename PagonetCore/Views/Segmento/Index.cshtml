@model IEnumerable<PagonetCore.Models.AdSegmento>

@{
    ViewBag.Title = "Segmentos";
}

<h2>Segmentos</h2>

<p>
    @Html.ActionLink("Crear", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.co_seg)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.seg_des)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.importado_web)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.importado_pro)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.co_seg)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.seg_des)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.importado_web)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.importado_pro)
        </td>
        <td>
            @Html.ActionLink("Modificar", "Edit", new { id=item.id_segmento }) |
            @Html.ActionLink("Detalles", "Details", new { id=item.id_segmento }) |
            @Html.ActionLink("Eliminar", "Delete", new { id=item.id_segmento })
        </td>
    </tr>
}

</table>
